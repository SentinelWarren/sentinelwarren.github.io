---
navigationLabel: Home
---

import { format } from "date-fns";
import { mix } from "@theme-ui/color";
import Footer from "../components/footer";
import Socials from "../components/socials";

export const fav_techs = [ "Python", "C", "JavaScript | Typescript", "Powershell", "Deno", "Nodejs", "Vue.js", "React.js", "Gatsby", "HTML5", "CSS3", "C#", "R", "Rust", "Ruby", "Blender", "Unity 3d", "Unreal Engine", "PostgreSQL", "GraphQL", "Arduino", "Raspberry Pi", "Sonic Pi"]

# habari_dunia
<Message variant="success" mb={2}>
  <Text variant="styles.h4">Mambo, my name is Warren Kalolo with alias SentinelWarren!</Text>
  I manage IT infrastructures and develop software, ML models and WebApps based on different tech stacks, the main being; Python, Javascript | Typescript [Vue.js, Vuetify, React.js, Gatsby], HTML+CSS, C# & Powershell. I am also a 3D artist hobbyist #TeamBlender3D #TeamUnity3D #TeamUnrealEngine.
  <Divider/>
  <Button as="a" mr={2} href="documents/WarrenKalolo.pdf" variant="primary" role="button">Resume📄</Button>
  <Link href="documents/WarrenKalolo.txt" role="button">.txt version</Link>
</Message>

<Message variant="success" mb={2}>I fully believe in Emerging technologies especially when it comes to African industrial revolution & digitization context. I root for Open-source Software; and not just Open-source but rather the Stallman's GNU movement. Most of my spare time is spent contemplating the state of Philosophy, Quantum Physics, Neuroscience, Carl Jung's work on Psychoanalysis and other Analytical psychology shenanigans i.e MBTI.</Message>
<Divider/>
<Grid gap={2} columns={[1, "1fr 2fr"]}>
  <Box>
    <Heading variant="styles.h2">get_in_touch</Heading>
    <Socials/>
  </Box>
  <Box>
    <Heading variant="styles.h2">favorite_tech</Heading>
    {fav_techs.map((fav_tech, index) => {
        return (
          <Badge
            key={index}
            variant="primary"
            sx={{
              mr: 2,
              mb: 2,
              color: mix("secondary", "success", `${index / fav_techs.length}`),
              borderColor: mix(
                "secondary",
                "success",
                `${index / fav_techs.length}`
              ),
            }}
          >
            {fav_tech}
          </Badge>
        )
      })
    }
  </Box>
</Grid>
<Divider/>
<Heading variant="styles.h2">fetch_projects</Heading>
<SourceList filter="projects">
  {projects => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: theme => `-${theme.space[2]}px`,
        mr: theme => `-${theme.space[2]}px`,
      }}
    >
      {projects.splice(0, 3).map((edge, index) => {
        const {
          frontmatter: { title, date, tags },
          fields: { slug },
        } = edge.node
        return (
          <Box
            key={index}
            sx={{ mb: 3, maxWidth: ["100%", "50%", "33.333%"], width: ["100%", "50%", "33.333%"] }}
          >
            <Link href={slug} sx={{textDecoration: "none"}}>
              <Card
                sx={{
                  ml: 2, 
                  mr: 2,
                  p: 3,
                }}
              > 
                <Text sx={{fontSize:0, color:"muted"}}>{format(new Date(date), "d-MMM-u")}</Text>
                <Heading variant="styles.h4" sx={{color: "text"}}>{title}</Heading>
                {tags.map((tag, index) => {
                  const { name, count, percent } = tag
                  return (
                    <Badge
                      key={index}
                      variant="primary"
                      sx={{
                        mr: 2,
                        mb: 2,
                        color: mix("success", "secondary", `${index / tags.length}`),
                        borderColor: mix(
                          "success",
                          "secondary",
                          `${index / tags.length}`
                        ),
                      }}
                    >
                      {tag}
                    </Badge>
                  )
                })}
                <Text sx={{textDecoration: "underline"}}>View project</Text>
              </Card>
            </Link>
          </Box>
        )
      })}
    </Flex>
  )}
</SourceList>
<Flex 
  sx={{
    justifyContent: "center",
  }}
>
  <Button as="a" mr={2} href="/projects" variant="primary" >Show more!! 👨‍💻👩‍💻</Button>
</Flex>
<Divider/>
<Heading variant="styles.h2">fetch_recent_posts</Heading>
<SourceList filter="posts">
  {posts => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: theme => `-${theme.space[2]}px`,
        mr: theme => `-${theme.space[2]}px`,
      }}
    >
      {posts.splice(0, 4).map((edge, index) => {
        const {
          frontmatter: { title, date, tags },
          fields: { slug },
        } = edge.node
        return (
          <Box
            key={index}
            sx={{ mb: 3, maxWidth: ["100%", "50%"], width: ["100%", "50%"] }}
          >
            <Link href={slug} sx={{textDecoration: "none"}}>
              <Card
                sx={{
                  ml: 2, 
                  mr: 2,
                  p: 3,
                }}
              > 
                <Text sx={{fontSize:0, color:"muted"}}>{format(new Date(date), "d-MMM-u")}</Text>
                <Heading variant="styles.h4" sx={{color: "text"}}>{title}</Heading>
                {tags.map((tag, index) => {
                  const { name, count, percent } = tag
                  return (
                    <Badge
                      key={index}
                      variant="primary"
                      sx={{
                        mr: 2,
                        mb: 2,
                        color: mix("success", "secondary", `${index / tags.length}`),
                        borderColor: mix(
                          "success",
                          "secondary",
                          `${index / tags.length}`
                        ),
                      }}
                    >
                      {tag}
                    </Badge>
                  )
                })}
                <Text sx={{textDecoration: "underline"}}>View post</Text>
              </Card>
            </Link>
          </Box>
        )
      })}
    </Flex>
  )}
</SourceList>
<Flex 
  sx={{
    justifyContent: "center",
  }}
>
  <Button as="a" mr={2} href="/posts" variant="primary">Read more 📑</Button>
</Flex>
<Divider/>
<Footer/>
